# CLAUDE.md

See [`README.md`](README.md) for the project overview, the `make` targets to run and test the
stack (`dev-db`, `dev-backend`, `dev-frontend`, `up`, `test`, `lint`), the folder structure, and
the technologies used.

See [`docs/decisions/`](docs/decisions/) for the rationale behind those choices.

## Code style

- No self-explanatory comments. If a comment feels necessary, the code's naming or structure
  needs improving instead.
- 4-space indentation for Python. 2-space indentation for TypeScript, YAML, and Markdown. Tab for makefiles.
